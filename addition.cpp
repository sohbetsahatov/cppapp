#include <iostream>

using namespace std;

int main() {
   
    string names[3];

    getline(cin, names[0]);
    getline(cin, names[1]);
    getline(cin, names[2]);

    cout << names[0];

        system("PAUSE");

    return 0; 
}